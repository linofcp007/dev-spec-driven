---
description: Short alias for /spec — start/resume the dev-spec-driven workflow. Atalho. Atajo.
argument-hint: "[feature idea or name]"
---

Use the **dev-spec-driven** skill. This is the short alias for `/spec`.

Feature / request: $ARGUMENTS

Begin at Phase 0 (Classification) — pick Vibe vs Spec mode and the composable track set
(core/+tdd/+saas/+ai) via `spec_classify`, then run the phased pipeline. If a `.specs/<feature>/`
already exists, run `spec_status` and resume. Respond in the user's language (EN/PT/ES).
